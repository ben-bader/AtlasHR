apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: hrms
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16
          env:
            - name: POSTGRES_DB
              value: hrms_auth
            - name: POSTGRES_USER
              value: hrms
            - name: POSTGRES_PASSWORD
              value: hrms
          ports:
            - containerPort: 5432